"use client";

export default function PitchPage() {
  return (
    <div className="fixed inset-0 bg-white overflow-y-auto snap-y snap-mandatory">
      <section className="h-screen snap-start flex items-center justify-center">
        <h1 className="text-6xl font-bold">Surely</h1>
      </section>
      <section className="h-screen snap-start flex items-center justify-center">
        <h2 className="text-4xl">Problem</h2>
      </section>
    </div>
  );
}
